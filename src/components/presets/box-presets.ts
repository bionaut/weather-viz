export const boxShadow = {
  boxShadow: '0 0px 5px -2px rgba(0,0,0, 0.3)',
}

export const fullSize = {
  height: '100%',
  width: '100%',
}

export const tag = (tooltip?: boolean) => ({
  height: tooltip ? '22px' : '30px',
  m: 2,
  minWidth: '33px',
})

export const border = {
  border: '1px solid',
  borderColor: 'border',
}

export const borderBottom = {
  borderBottom: '1px solid',
  borderColor: 'gray.4',
}
